apiVersion: kuttl.dev/v1
kind: TestAssert
timeout: 100
collectors:
  - command: kubectl get splitdns/shdc-psmdb -n ${NAMESPACE} -o yaml
  - command: kubectl get secret/shdc-secret-1 -n ${NAMESPACE} -o yaml
  - type: pod
    namespace: everest-system
    selector: control-plane=controller-manager
    tail: 100
commands:
  - command: kubectl wait --for=delete splitdns/shdc-psmdb -n ${NAMESPACE}
  - command: kubectl wait --for=delete secret/shdc-secret-1 -n ${NAMESPACE}
