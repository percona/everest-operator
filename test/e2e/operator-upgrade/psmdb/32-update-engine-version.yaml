apiVersion: kuttl.dev/v1
kind: TestStep
timeout: 10
commands:
  - command: kubectl patch dbc/test-psmdb-cluster -n $NAMESPACE --type=merge -p '{"spec":{"engine":{"version":"${PSMDB_DB_ENGINE_VERSION}"}}}'
