apiVersion: kuttl.dev/v1
kind: TestStep
timeout: 30
commands:
  - command: kubectl -n ${NAMESPACE} delete db/test-pg-cluster --wait=false
  - command: kubectl delete pod/pg-client -n ${NAMESPACE} --wait=false
  - command: kubectl delete -n ${NAMESPACE} -f ${OPERATOR_ROOT_PATH}/tests/testdata/minio --wait=false
