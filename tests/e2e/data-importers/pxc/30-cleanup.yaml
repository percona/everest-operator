apiVersion: kuttl.dev/v1
kind: TestStep
timeout: 30
commands:
  - command: kubectl -n ${NAMESPACE} delete db/test-pxc-cluster --wait=false
  - command: kubectl delete -n ${NAMESPACE} pod/pxc-client --wait=false
  - command: kubectl delete -f ${OPERATOR_ROOT_PATH}/internal/data-importer/deploy/pxc --wait=false
  - command: kubectl delete -n ${NAMESPACE} job --all --wait=false
