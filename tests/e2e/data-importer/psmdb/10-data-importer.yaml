apiVersion: kuttl.dev/v1
kind: TestStep
---
apiVersion: everest.percona.com/v1alpha1
kind: DataImporter
metadata:
  name: test-psmdb-data-importer
spec:
  displayName: "Test PSMDB data importer"
  description: "Data importer for PSMDB E2E tests"
  supportedEngines:
  - psmdb
  jobSpec:
    image: "busybox"
    command: ["/bin/sh", "-c", "sleep 10 && cat \"$0\""]
