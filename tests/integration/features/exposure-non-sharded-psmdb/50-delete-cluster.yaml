apiVersion: kuttl.dev/v1
kind: TestStep
timeout: 30
commands:
  - script: kubectl -n $NAMESPACE delete db/psmdb-non-sharded-exposure psmdb/psmdb-non-sharded-exposure --wait=false && sleep 5
  - command: kubectl patch db/psmdb-non-sharded-exposure -n $NAMESPACE -p '{"metadata":{"finalizers":null}}' --type merge
  - command: kubectl patch psmdb/psmdb-non-sharded-exposure -n $NAMESPACE -p '{"metadata":{"finalizers":null}}' --type merge
