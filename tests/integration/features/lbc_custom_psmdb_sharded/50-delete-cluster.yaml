apiVersion: kuttl.dev/v1
kind: TestStep
timeout: 30
commands:
  - script: kubectl -n $NAMESPACE delete db/psmdb-lbc psmdb/psmdb-lbc --wait=false && sleep 5
  - command: kubectl patch db/psmdb-lbc -n $NAMESPACE -p '{"metadata":{"finalizers":null}}' --type merge
  - command: kubectl patch psmdb/psmdb-lbc -n $NAMESPACE -p '{"metadata":{"finalizers":null}}' --type merge
