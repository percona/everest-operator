apiVersion: kuttl.dev/v1
kind: TestStep
commands:
  - command: kubectl annotate db/test-pxc-cluster everest.percona.com/dbtemplate-kind- -n "${NAMESPACE}"
  - command: kubectl annotate db/test-pxc-cluster everest.percona.com/dbtemplate-name- -n "${NAMESPACE}"
