apiVersion: kuttl.dev/v1
kind: TestAssert
timeout: 400
commands:
  # check that there is no dbb/pg-backup CRs left
  - script: >
      test "$(kubectl -n $NAMESPACE get dbb test-db-backup-cleanup --ignore-not-found=true" = ""

