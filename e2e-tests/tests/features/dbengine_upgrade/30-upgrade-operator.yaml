apiVersion: kuttl.dev/v1
kind: TestStep
timeout: 10
commands:
  - command: kubectl -n "${NAMESPACE}" annotate --overwrite=true dbengine percona-xtradb-cluster-operator everest.percona.com/upgrade-to=$PXC_OPERATOR_VERSION

