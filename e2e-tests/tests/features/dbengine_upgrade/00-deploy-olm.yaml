apiVersion: kuttl.dev/v1
kind: TestStep
timeout: 10
commands:
  # CRDs
  - command: kubectl apply --server-side -f https://raw.githubusercontent.com/operator-framework/operator-lifecycle-manager/master/deploy/upstream/quickstart/crds.yaml
  # Operator
  - command: kubectl apply --server-side -f https://raw.githubusercontent.com/operator-framework/operator-lifecycle-manager/master/deploy/upstream/quickstart/olm.yaml
---
