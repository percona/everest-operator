apiVersion: kuttl.dev/v1
kind: TestStep
commands:
  - command: kubectl -n $NAMESPACE delete db test-pg-cluster
  - command: kubectl patch postgresclusters test-pg-cluster -n $NAMESPACE -p '{"metadata":{"finalizers":null}}' --type merge
    ignoreFailure: true
  - command: kubectl -n kuttl-test-pg delete db test-pg-cluster
  - command: kubectl patch postgresclusters test-pg-cluster -n kuttl-test-pg -p '{"metadata":{"finalizers":null}}' --type merge
    ignoreFailure: true
