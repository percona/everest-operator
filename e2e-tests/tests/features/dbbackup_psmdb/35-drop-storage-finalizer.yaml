apiVersion: kuttl.dev/v1
kind: TestStep
commands:
  - command: kubectl patch psmdb-backup test-db-backup-cleanup -n $NAMESPACE -p '{"metadata":{"finalizers":null}}' --type merge
    ignoreFailure: true

