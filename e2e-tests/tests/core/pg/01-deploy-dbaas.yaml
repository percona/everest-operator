apiVersion: kuttl.dev/v1
kind: TestStep
timeout: 10
commands:
  - script: DEFAULT_NAMESPACE=$NAMESPACE ../../../../bin/manager -metrics-bind-address :0 -health-probe-bind-address :0
    background: true
    #skipLogOutput: true
